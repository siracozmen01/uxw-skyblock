# ==============================================================================
# UXPLIMA Skyblock - Anti-Alt & Starter Economy Protection Configuration (HOCON)
# Canonical Section 2.32: Hard reset inventory/enderchest purge,
# quarantine transfer window, reset cooldowns, daily limits, and co-op hopping lock.
# ==============================================================================
anti-abuse {
    # Strictly purge player inventory, equipment, offhand, ender chest, and experience on island reset/deletion
    # Neutralizes exploit vectors transferring starter items across repeated reset loops
    purge-inventory-on-reset = true

    # Quarantine duration for newly initialized islands
    # During quarantine: PlayerDropItemEvent disabled within boundary, visitor access locked
    quarantine-duration = "15m"

    # Cooldown required between island deletions / resets
    reset-cooldown = "12h"

    # Maximum permitted island resets within a rolling 24-hour window
    max-resets-per-day = 3

    # Window duration for daily reset limit
    reset-window-duration = "24h"

    # Co-op departure / kick quarantine prohibiting immediate recruitment into other islands
    coop-join-cooldown = "24h"

    # Permission nodes for administrative bypass
    reset-bypass-permission = "uxmskyblock.bypass.resetlimits"
    coop-bypass-permission = "uxmskyblock.bypass.coopcooldown"
    quarantine-bypass-permission = "uxmskyblock.bypass.quarantine"
}
