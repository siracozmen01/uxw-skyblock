package com.uxplima.uxmskyblock.core.domain.result;

/**
 * Empty success outcome representation for {@link Result Result&lt;Unit, E&gt;} when an operation
 * produces no return value but its success or failure must be handled exhaustively.
 */
public enum Unit {
    INSTANCE
}
